%h1 Listing messages
- if user_signed_in?
  Logged in as #{current_user.username}
  = link_to "Sign Out", destroy_user_session_path, method: :delete

%br
%br

.messages
  - @messages.each do |message|
    .message
      = message.username + ": "
      = message.content

- if user_signed_in?
  = render 'form'
- else
  %br
  Please #{link_to "sign in", new_user_session_path} to post a message.
  %br
  Not a member? #{link_to "Sign up!", new_user_registration_path}
